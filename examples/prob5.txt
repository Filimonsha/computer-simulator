(var a 2520)
(var b 11)
(var c 3)
(var e 2520)
(var d 11)
(loop secloop
    (loop firstloop
        (if (= b 0)
            (return-from firstloop)
        )
        (setvar c b)
        (setvar b (% a b))
        (setvar a c)
    )
    (setvar c (* (/ e a) d))
    (setvar e c)
    (setvar d (+ d 1))
    (setvar a e)
    (setvar b d)
    (if (= d 20)
        (return-from secloop)
    )
)
(printint c)
