(var a 1)
(var b 2)
(var c 2)
(var sum 2)
(loop firstloop
    (setvar c b)
    (setvar b (+ b a))
    (setvar a c)
    (if (> b 4000000)
        (return-from firstloop)
    )
    (if (= (% b 2) 0)
        (setvar sum (+ sum b))
    )
)
(printint sum)
